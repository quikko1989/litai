#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ng-html2js/bin/ng-html2js" "$@"
  ret=$?
else 
  node  "$basedir/../ng-html2js/bin/ng-html2js" "$@"
  ret=$?
fi
exit $ret
